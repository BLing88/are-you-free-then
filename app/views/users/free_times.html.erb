<%= form_with url: free_times_path, id: 'calendar-container' do |f| %>
    <%= tag.div id: "react-calendar-input",
      "data-free_times" => @free_times.to_json(only: [:start_time, :end_time])%>

    <%= f.submit "Submit" %>
<% end %>

