<svg class="app-logo" width="<%= width %>" 
                      height="<%= height %>"
                      viewBox="0 0 <%= width %> <%= height %>" >
     <text 
        x="0" 
        y="<%= 0.6 * height %>" 
        stroke="black"
        fill="none"
        font-size="<%= 0.7 * height  %>">
          W
     </text>
     <text
         x="<%= 0.3 * width %>"
         y="<%= 0.8 * height %>"
         fill="var(--action-color)"
         opacity="0.35"
         font-size="<%= 0.7 * height %>"
         >
           2
     </text>
     <text
         x="<%= 0.48 * width %>"
         y="<%= 0.7 * height %>"
         fill="var(--action-color)"
         opacity="0.35"
         font-size="<%= 0.7 * height %>"
         >
           M 
     </text>
     <text 
         x="<%= 0.75 * width %>" 
        y="<%= height - 5 %>" 
        stroke="black"
        fill="none"
        font-size="<%= 0.7 * height  %>">
          2
     </text>

</svg>
