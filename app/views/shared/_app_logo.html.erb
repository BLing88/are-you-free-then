<svg class="app-logo" width="<%= width || 350 %>" 
                      height="<%= height || 72 %>"
                      viewBox="0 0 <%= width || 350 %> <%= height || 72%>" >
      <text x="4" y="50" fill="var(--action-color)" opacity="1" stroke="black" font-size="42" font-weight='bold'>Are You Free Then</text>
</svg>
