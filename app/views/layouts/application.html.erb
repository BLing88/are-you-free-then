<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/a10f9eadbe.js" crossorigin="anonymous"></script>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="<%= current_page?('/') && !logged_in? ? 'welcome' : '' %>">
    <%= render 'layouts/header' unless !logged_in? %>
    <% flash.each do |message_type, message| %>
        <%= render 'layouts/flash_message', message: message, type: message_type %>
    <% end %>
    <%= yield %>
    <%#= debug(params) if Rails.env.development? %>
  </body>
</html>
